server:
  port: 8080
  servlet:
    context-path: /

spring:
  application:
    name: nzq-blog-backend

logging:
  level:
    com.nzq.blog: DEBUG
    org.springframework.web: INFO
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"